public with sharing class OrderTriggerHandler {
    public static void createAssetRecord(){
        // List<OrderProduct> orderProductList = [SELECT Quantity, Product2Id.Family  FROM OrderItem ];
    }
}